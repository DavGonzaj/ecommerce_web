// Views/Admin/Products.cshtml
@model List<Product>
<h2>Products</h2>
<table>
    @foreach (var product in Model)
    {
        <tr>
            <td>@product.Name</td>
            <td>$@product.Price</td>
            <td>@product.Stock</td>
        </tr>
    }
</table>